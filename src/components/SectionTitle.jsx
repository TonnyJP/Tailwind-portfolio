export const SectionTitle = ({title}) => {
    return (
        <div className="border-b border-gray-200 pb-5">
            <h2 className="capitalize text-3xl font-medium tracking-wide">{title}</h2>
        </div>
    )
}